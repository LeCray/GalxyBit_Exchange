<table class="table" ">
	<thead>
		<tr>
			<th><b>DATE OF TRANSACTION</b></th>
			<th><b>TYPE</b></th>
			<th><b>AMOUNT</b></th>
			<th><b>STATUS</b></th>
			<th><b>ID</b></th>
		</tr>
	</thead>
	<tbody>
		<% @zar_transactions.each do |t| %>
			<tr>
				<td><%= t.created_at.strftime("%Y-%m-%d || %X") %></td>
				<td><%= t.transaction_type %></td>
				<td><%= number_to_currency(t.amount, unit: "R ")  %></td>
				<td><%= t.status %></td>
				<td><%= t.id %></td>
			</tr>
		<% end %>
		<tr>
			<th><b>Balance: <%= number_to_currency(@account.zar_balance, unit: "R ")%></b></th>
			<th></th>
			<th></th>
			<th></th>
			<th></th>
		</tr>
	</tbody>
</table>
<div style="text-align: center; top:">
  <%= will_paginate @zar_transactions, renderer: BootstrapPagination::Rails %>
</div>
